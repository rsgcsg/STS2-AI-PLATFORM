{
  "name": "@rsgcsg/sts2-platform-live-ui",
  "version": "0.1.0-rc.1",
  "private": true,
  "description": "Unified in-game STS2 AI Platform live control and diagnostics UI",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "check": "node --test test/*.test.mjs",
    "build": "node build.mjs",
    "doctor": "node deploy.mjs doctor",
    "deploy": "node deploy.mjs deploy",
    "verify:loaded": "node deploy.mjs verify-loaded",
    "rollback": "node deploy.mjs rollback"
  }
}
